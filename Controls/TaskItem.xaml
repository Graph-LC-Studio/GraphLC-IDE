<UserControl x:Class="GraphLC_IDE.Controls.TaskItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GraphLC_IDE.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="content" x:FieldModifier="private" Grid.Column="0"/>
            <TextBlock x:Name="description" x:FieldModifier="private" Grid.Column="1"/>
            <TextBlock x:Name="closeButton" x:FieldModifier="private" Grid.Column="3" Text="×" Cursor="Hand" MouseLeftButtonUp="CloseButtonClick"/>
        </Grid>
    </Grid>
</UserControl>
