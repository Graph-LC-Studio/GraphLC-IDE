<UserControl x:Class="GraphLC_IDE.Controls.WindowTitle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GraphLC_IDE"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="layoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="3"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" x:Name="layoutTitle">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="13"/>
                <ColumnDefinition Width="3"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="title" Grid.Column="1"/>
            <TextBlock x:Name="closeButton" Grid.Column="2" Text="×" Cursor="Hand" MouseLeftButtonUp="CloseButtonClick"/>
        </Grid>
    </Grid>
</UserControl>
